（練習）

Windows 関連

・Wslのインストール方法
Power shallを管理者として開く。
Wsl —install
Wsl —status

・ubuntuのインストール
Wsl —install -d Ubuntu

・以降、ターミナルを開く際は、スタート画面でUbuntuで検索

・PAHT外の実行ファイルの実行
./(ファイル名：例：python3)　←　パスを教えてあげる必要がある。

・１次的に(例：myscriptを)PATHへ設定
Exporte PATH=
(使用例：export PATH=$PATH: ~/pracice/sample-dir)
実行：my script

・windows内でのファイル等を、linuxで実行する場合はパスが変わる。
/mnt/c/User/Desktop/~~~~~~
で指定してあげる。

・Dockerのインストール
①PCのシステムの確認
・設定→システム→バージョン情報→システムの種類を確認
X64 ベースプロセッサ：AMD
ARMベースプロセッサ：ARM

②docker のサイトから①で確認したやつをダウンロードして、再起動

③スタート画面からdockerを開く
選択画面が出てきたら、use WSL 2~~~ってやつを選択。
④利用規約に同意
⑤dockerを開いて、上部の歯車→Resources→WSL integration→Enable integration with ~~と、Ubuntuにチェックを入れて、Apply&restart
⑥：確認作業
ターミナル開いて、docker —versionで確認


GIT関連の登録

・gitが入っているかの確認
Which git

Gitの名前と、アドレスの設定。
Git config  - - global user.name　“名前”
Git config - - global user.email　“アドレス”

・gitの秘密の鍵と公開鍵の設定方法
①Ssh-keygen -t ed25519 -C “アドレス”
②：①実行後に、出てくる最初のenter file ~~~では、ただenterを打つ
③：自分で設定するpwを入力してenter。もう1度、確認でenter
④：公開鍵をgithubにアップ
cat ~/.ssh/id_ed2551-.pub　：　公開鍵の確認
⑤：git hubアカウントに行ってSSH and GPG keysの登録を選択
⑥：new key推してタイトルを入力。typeはAUthenticationのまま。keyに⑤の内容を貼り付け。
⑦：初回接続・shh -T git@github.comを押して次に出てくるのをenter



Python の確認
①：インストールされているか確認方法
With python


Venvの環境構築
・環境構築
Python3 -m venv vent
（たまに、venvを使うためのパッケージが必要とでるその際は以下のコーデでインストール）
Sudo apt update
Sudo apt install python3.14 -ven



VScode　

・ブラウザからインストール
https://code.visualstudio.com/

インストール後、ファイルを開いてPATHとホーム画面への追加をしておくと便利


Pytest

・ライブラリーとして、pytestをインストール
・注意事項
- ファイルの名前をtest_にする。
- 関数名もtest_の形にする。
これで、uv run pytestの様に実行すると、自動的にtestとある、ファイルを探してくれて、testとある関数を実行してくれる。






"""自分"""






Git 

・git add で今いるリポジトリ以下の全てをステージに上げる方法 git add .

・Git add でステージにあげたものを、下ろす方法
Git rm —cached ファイル名

・ブランチ一覧表示
Git branch

・branch名の変更
Git branch -m （古いブランチ名）　（新しいブランチ名）

・commitの履歴確認 git log  - - oneline - - graph - - all




Git Hub
・自分のディレクトリと、githubのリポジトリを紐付け
① git remote add origin github@・・・
Git remote -v:確認
↑：自分のリポジトリを、今後github上で共有していこうとする場合。 ↓：githubにあるリポジトリを、自分のリポジトリと紐付けてpullする方法 git clone

②
Git push -u origin main.  (branchが複数あれば、branchごとにpush)
(3)
Cloneした場合、リモート側で作られたbranchは、一緒にcloneされない。clone直後に以下のコードを行うと、それをローカルに持って来れる。 git switch （ブランチ名）



プルリクの流れ

・ターミナル
Git push -u origin (作業ブランチ名)
・git hub
今回の、pullの先を確認して、実行。




Venv
・環境の作り方
Python3 -m vent vent

・requirement.txtへの落とし方
Pip freeze >  requirement.txt

・requirement.txt内のもののインストール方法
Pip install -r requirement.txt

・環境の削除
Rm -rf (環境名)







UV
・Uvのダウンロード：ディレクトリとかの指定は関係ない。
curl -LsSf https://astral.sh/uv/install.sh | sh

・インストール後に反映されない可能性がある。その場合は、ターミナルを閉じて開き直す。

・uvは、ubuntuと独立したpythonのため、uvでのpythonのインストールが必要。
・インストール方法
Uv python install (version番号：例　3.12)


・ディレクトリに移動し、uvのファイルの作成。
cd ~/practice 
uv init (プロジェクト名) 
cd （プロジェクト名）

・他の人のライブラリーを引き継ぐ方法
(Pip install -r requirement.txt のuvバージョン）
Uv sync

・uvは、activateの様に環境を有効にする段階を踏まなくていい。
Uv run python (実行したいファイル名)
これで、uv経由で一気に実行。


・ライブラリーの追加
Uv add (ライブラリー名)

・uv環境の有効化（環境名は自動で.venvになっている。）
Source .venv/bin/activate

・実行
Uv run python (ファイル名)


UVでのjupyterの追加

・追加方法
Uv add Jupyter 

・開き方
Uv run jupter notebook
- 自動で開くか、urlが出てくる（https://localhost;888\  ~~~~~~~~~）





Docker

【基本】
・インストール
https://www.docker.com/products/docker-desktop/

・すでにあつイメージの確認
Docker images

・すでにあるイメージをコンテナに入れて起動。（対話モード）
Docker run -it (イメージ名)　python３

・抜け方
exit()  【チーム開発】
①：Dockerfileの作成。作業しているディレクトリー内に作成。
Touch Dockerfle

②Doclerfileの書き込み
 FROM (イメージ名)
WORKDIR (コンテナ内でのディレクトリ名)：コンテナ内でのディレクトリ作成と移動をしてくれる。
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . . (最初の.が、ローカルのディレクトリの場所。次の.がコンテナ内の場所)
CMD[“python”, “app,py”] 実行。


③；imageの作成
Docker built -t (イメージ名：自分で作成)　.  :(場所)
Docker imagesでimageができたか確認  ④；実行
Docker run （イメージ名）


Docker Compose 
複数のコンテナを連携して動かす技

実行する方法
①：compose.yamlファイルを作る。
②：compose.yamlの書き方は、参照。
③：実行　
docker compose up -d
④；起動した複数コンテナを停止させる方法
Docker compose down 
⑤起動中に、各コンテナのいろいろな情報の確認
⑴：起動しているか
Docker compose ps
⑵：何を出力しているか
Docker compose logs



GitHum Action 
やってくれること：pushやPRを行った際に、これが正しく実行される正しい内容であるかをgithub上で自動でやってくれるもの

①ディレクトリの作成
Mkdir -p .github/workflows
Touch ./.github/worlflows/test.yaml　の作成

②test.yamlに指示を記入
③実行：これは単純にアドしてcommitとpushを行うだけ。
④確認：github上で　actionsを選択して確認。
